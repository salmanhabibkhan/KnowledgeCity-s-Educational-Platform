import { NestedStack, NestedStackProps } from 'aws-cdk-lib';
import { Construct } from 'constructs';
import { ICluster } from 'aws-cdk-lib/aws-eks';
import { ClusterInfo, NestedStackBuilder } from '../../spi';
import { NestedStackAddOn } from '../nested-stack';
export declare const GatewayCrdChannels: {
    STANDARD: string;
    EXPERIMENTAL: string;
};
export interface GatewayApiCrdsAddOnProps extends NestedStackProps {
    gatewayApiCrdsUrl?: string;
    cluster?: ICluster;
}
export declare class GatewayApiCrdsStack extends NestedStack {
    readonly manifestIds: string[];
    constructor(scope: Construct, id: string, props?: GatewayApiCrdsAddOnProps);
}
export declare class GatewayApiCrdsBuilder implements NestedStackBuilder {
    private readonly props?;
    readonly options: GatewayApiCrdsAddOnProps;
    constructor(props?: GatewayApiCrdsAddOnProps | undefined);
    build(scope: Construct, id: string, nestedStackProps?: NestedStackProps): NestedStack;
}
export declare class GatewayApiCrdsAddOn extends NestedStackAddOn {
    private readonly options?;
    constructor(props?: GatewayApiCrdsAddOnProps);
    deploy(clusterInfo: ClusterInfo): void | Promise<Construct>;
}
